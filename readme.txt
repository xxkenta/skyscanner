Make sure node is installed

Clone the repository, cd into it, and run "npm install"

make sure to read the comments in scanner.js and adjust the paramaters accordingly.

then run "node scanner.js"

It will take a while because of the limits of the free API, but results will be put in "price-data.json"

